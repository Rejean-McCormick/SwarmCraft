version: '3'
services:
  backend:
    build: ./scratch/backend
    ports:
      - '3001:3001'
    environment:
      - DB_PATH=/data/store.sqlite
  frontend:
    image: nginx:alpine
    ports:
      - '8080:80'
