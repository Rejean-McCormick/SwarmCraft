version: '3.8'
services:
  backend:
    build: ./scratch/backend
    ports:
      - '3001:3001'
  frontend:
    image: node:18
    ports:
      - '5173:5173'
  db:
    image: nouchka/sqlite3
    volumes:
      - db-data:/data
volumes:
  db-data:
