# Batch Endpoints OpenAPI Scaffolding

This document outlines the OpenAPI scaffolding for the MVP v2 batch endpoints. It references the same jokegen.yaml draft at scratch/openapi/jokegen.yaml and is intended to keep parity between code and docs.
